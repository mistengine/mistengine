<% cache article do %>
  <article>
    <h2><%= link_to article.title, article %></h2>
    <%= simple_format(article.blurb) %>
    <%= t('articles.byline', name: article.byline, date: article.created_at.to_time.strftime("%B %d, %Y")) %>
  </article>
<% end %>
