<% cache article do %>
  <article>
    <h2><%= link_to article.title, article %></h2>
    <%= simple_format(article.blurb) %>
    by <%= article.byline %> on <%= article.created_at.to_time.strftime("%B %d, %Y") %>
  </article>
<% end %>
